# VM-side dependencies (Ubuntu, ROS2 Kilted)
# venv: ~/venvs/ackerman_ros2, created with --system-site-packages
#   (required so rclpy, installed via apt, remains importable inside the venv)
#
# ROS2 itself (rclpy, ament tooling, etc.) comes from apt, not pip —
# not listed here. This file covers only what pip installs into the venv.

empy==3.3.4
# rosidl_adapter (ROS2's .msg -> IDL converter) imports this at build time.
# Without it, `colcon build` fails on any package with custom interfaces:
#   ModuleNotFoundError: No module named 'em'
# Must be empy, not the unrelated PyPI package named `em`.
# empy 4.x changed its API and breaks current rosidl tooling — pin to 3.3.4.

pytest
numpy

# carsim_bridge/perception_node.py (M2): CPU-only torch, no CUDA/MPS on this
# VM. perception/model/preprocess.py needs PIL; pyyaml is only needed on the
# training side (perception/model/train.py reads training_config.yaml),
# not here. Pin loosely against requirements-sim.txt's Mac-side torch==2.13.0
# -- exact patch match isn't required for inference-only CPU use, but keep
# the major version aligned if this ever needs bumping.
torch==2.13.0
pillow

# control/mpc/* (M3, ADR-19 onward) and carsim_bridge/mpc_node.py: the
# acados/CasADi OCP solver, plus mujoco for control/mpc/closed_loop_sim.py's
# standalone diagnostic (no perception/ROS2 in that loop, but it needs
# physics -- mujoco normally only runs on the Mac side of the ZMQ bridge
# split, ADR-1/ADR-2, this is the one place it also runs here). Not
# previously tracked in this file even though installed during M3's OCP
# work -- backfilled now while adding mpc_node's own dependency on the
# same two.
casadi==3.7.2
mujoco==3.12.0
# acados_template is NOT pip-installable from PyPI here -- it's installed
# in editable mode (`pip install -e`) from a local clone of the acados
# source repo (ACADOS_SOURCE_DIR=$HOME/acados, built by the user beforehand
# per docs/decisions.md's M3 OCP task), so there's no version pin to give
# here beyond noting the installed version was 0.5.1 at the time this was
# written. Not resolvable via a plain `pip install -r` on a fresh VM --
# see docs/decisions.md's M3 OCP formulation task for the acados
# clone+build+install steps.
